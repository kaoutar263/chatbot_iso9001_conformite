version: '3.8'

services:
  chatbot:
    build: .
    container_name: chatbot_app
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
      - ./.env:/app/.env
    environment:
      - DATABASE_URL=sqlite:///./data/chat.db
      - LLM_PROVIDER=groq
